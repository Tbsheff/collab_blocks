{
    "name": "@collabblocks/edge-worker",
    "version": "0.1.0",
    "description": "Edge WebSocket proxy using Cloudflare Workers & Durable Objects",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc -b",
        "dev": "wrangler dev src/index.ts",
        "deploy": "wrangler deploy src/index.ts",
        "test": "vitest run"
    },
    "dependencies": {
        "@cloudflare/workers-types": "^4.20240627.0",
        "@collabblocks/auth": "workspace:*",
        "@collabblocks/protocol": "workspace:*"
    },
    "devDependencies": {
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.8.3",
        "vitest": "^3.1.0",
        "wrangler": "^3.0.0"
    }
}